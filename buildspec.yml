version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - ls -al
      - sudo npm install

  build:
    commands:
      - sudo npm run build
      - sudo ls -al

artifacts:
  files:
    - '**/*'
  name: AutoCrashData
