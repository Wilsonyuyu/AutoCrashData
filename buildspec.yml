version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - ls -al
      - npm install

  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  name: AutoCrashData
